---
# Vars file for mt_runner role.
# Note: Keep YAML keys sorted alphabetically.
mt_runner_mt_files:
  4:
    terminal: terminal.exe
  5:
    terminal: terminal64.exe

mt_runner_terminal_errors_regex: >-
  (exit with code -|Invalid account|not found|tester didn't start|tester not started)
